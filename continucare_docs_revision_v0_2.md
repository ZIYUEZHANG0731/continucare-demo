# ContinuCare 文档修订包 v0.1 → v0.2

本修订包按上一轮评审意见整理，**按目标源文件组织**，每节内容可直接合并进对应的 `docs/*.md`。修订分三类：

- 【修复】改正文本错误
- 【重写】整体替换原有小节
- 【新增】追加的小节或新文件

建议合并顺序：先做全局修复（第一部分），再合 06 安全与治理的三处（第五、六、七部分，互相引用），然后新建评测文档（第八部分），最后更新 00/01/05/09/10。

---

## 一、全局修复【修复】

### 1.1 Summary 示例文本截断

**位置**：`07_workbench_and_patient_app.md` §6.2、`08_demo_script.md` Step 7（两处为同一示例）

两处示例均在"患者复诊前主要问题是担心恶心是否会"处被截断。替换为完整版本：

> 过去14天患者完成11/14次随访。初期出现轻中度恶心和食欲下降，第6天报告呕吐一次并触发L2 Alert，护士当日电话随访处理，此后未再报告呕吐。体重从82.4kg降至81.1kg（-1.3kg，约-1.6%）。患者复诊前主要问题：担心恶心是否会长期持续，以及是否需要调整用药（AI已说明须由医生判断，并记入医生待确认事项）。数据缺失：Day 4、5、7、8 未提交记录。医生待确认：Day 3"食欲明显下降"为患者自报原话，未量化。

这个完整版顺带演示了三个设计点：负值趋势带百分比、患者问药被正确转成"待确认事项"、缺失日期明示。答辩演示时值得停留讲一下。

### 1.2 风险等级定义收敛为单一来源

L0-L4 的完整定义目前在 PRD §8、`04_pathway_engine.md` §9、`08_demo_script.md` 三处重复。保留 **PRD §8 作为唯一权威定义**，其余两处改为一句引用：

> 风险等级定义与对应动作见《01 产品需求文档》§8。

原因：未来调整任一等级的阈值或动作时，多处副本几乎必然改漏，造成文档内部矛盾——这类矛盾恰好是评委和临床审核最容易抓到的。

### 1.3 保真度术语统一

第 11 章标题为"中保真线框图"，副标题与正文多处写"低保真"。建议**全文统一为"中保真"**：Figwright 重绘稿已包含布局、组件层级和状态标签，超出低保真范畴。

同时，正文中对话示例、摘要示例前的 `Low-fidelity wireframe` 标签与线框图无关，统一改为 **"示例文案"**，避免概念混用。

---

## 二、00 产品总览：竞品分析增补国内厂商【新增】

**位置**：`00_overview.md` §10 竞品表格追加以下行

| 对象 | 主要能力 | 局限 | 我们的差异 |
|---|---|---|---|
| 讯飞医疗（智能随访/语音外呼类） | 语音外呼、规模化随访触达、问卷采集 | 以单向问卷和外呼为主，长期记忆、证据链摘要与复诊工作流闭环弱 | 患者自由表达转结构化Observation，复诊前生成带证据链的Summary |
| 微脉（全病程管理） | 个案管理师人力服务 + 患者运营 | 人力密集型，AI结构化与可审计性弱，难以标准化复制 | 把人力服务沉淀为可配置Pathway + 规则引擎 + 审计闭环 |
| 智云健康（慢病管理） | 院内外慢病管理、设备数据、药事生态 | 偏患者侧健康管理与药械生态，非科室复诊工作流 | 聚焦科室级照护路径与复诊准备，服务医生的临床上下文 |
| HIS厂商随访模块 | 短信/电话模板问卷 | 数据非结构化、无风险分级闭环、无摘要 | LLM理解 + 规则分级 + Timeline + Summary 完整闭环 |

> ⚠️ 以上定位为公开资料层面的概括。**答辩前需逐一核实各厂商最新产品能力**（官网、公开演示、招股书/年报），避免现场被反驳。

**同步并入 `09_pitch_materials.md` §8 竞品问答**：

**Q：国内已经有随访和全病程管理厂商，你们和他们什么区别？**
现有方案是两个极端：模板问卷（数据是死的、无闭环）或人力个案管理（贵、难复制）。我们的差异是三件事的组合：患者自由表达被转成带证据的结构化数据；风险由可审计的规则引擎分级进入医院任务流；复诊前生成可追溯的Summary。一句话：竞品做触达，我们做**触达之后的数据资产和复诊闭环**。

---

## 三、01 PRD：新增 §12 医生采纳机制【新增】

**位置**：`01_prd.md` 末尾新增一节。回应本产品最大的商业假设——医生是否愿意在复诊前打开Summary。

### 12.1 设计原则

Summary 的打开成本必须趋近于零。不能要求医生为它多登录一个系统、多记一个入口。

### 12.2 三条到达路径（按试点条件选择）

1. **工作站嵌入（首选）**：与 HIS 复诊叫号联动。患者签到后，Summary 以侧栏或弹层出现在医生工作站，一屏可读，点击展开证据链。
2. **诊前推送**：复诊前一晚，将"一页版Summary"推送至医生企业微信/钉钉/院内App。正文控制在 8-10 行，30 秒内读完。
3. **纸质兜底（试点期最低门槛）**：门诊护士站晨间批量打印当日复诊患者的Summary，夹入病历。技术含量最低，但可以最快验证"医生是否觉得有用"这个核心假设。

### 12.3 先护士后医生

第一阶段可量化的价值先落在护士侧：人工随访工时下降、异常处理SLA可追踪。医生侧价值用打开率和评分数据逐步证明，避免产品成败一开始就押在改变医生行为上。

### 12.4 采纳指标

- Summary 打开率（按复诊人次计）
- 打开时点与复诊开始时间的间隔
- 平均停留时长
- 医生"有帮助"评分与编辑率（编辑率高可能意味着内容不达预期，也可能意味着深度使用，需结合访谈判读）

---

## 四、05 AI Agent：新增 §11 为什么是多Agent【新增】

**位置**：`05_ai_agents.md` 末尾新增。评委高概率追问"一个大模型加工具调用为什么不行"，这节给出正面回答。

多 Agent 的目的不是智能，而是**治理**。医院环境要求"谁、在什么授权下、用什么工具、产出了哪条内容"可追溯。Agent 是本系统的职责单元：

- 每个 Agent 有独立的工具白名单：Care Agent 永远碰不到 Alert 关闭权限，Summary Agent 永远发不出患者消息。
- 每个 Agent 有独立的 Prompt 版本与评测集，可以单独回归、单独降级。
- 审计日志按 Agent 归属，安全事件可定位到单一职责边界内。

**实现层的诚实说明（答辩可直接使用）**：比赛原型中，Care/Memory/Risk 可共享同一底座模型，通过任务配置与工具权限区分。"多Agent"是逻辑与治理架构，不等于必须部署多个模型。单模型加工具调用在 Demo 层面可行，但会把所有权限集中在同一个上下文里——审计和降级都失去了边界。

---

## 五、06 安全与治理：重写 §6 Confidence 体系【重写】

**位置**：整体替换 `06_safety_and_governance.md` §6。原版直接使用模型输出的 0-1 置信度做治理决策，这是全文档技术上最站不住的一环。

### 6.1 原则

**不将模型自报的置信度作为治理依据。** LLM 输出的置信分数不可校准，直接用于"低置信转人工"会产生虚假的安全感。治理一律使用离散的 `confidence_tier`，由确定性信号组合判定。

### 6.2 Extraction confidence_tier 判定

| tier | 判定条件 | 治理动作 |
|---|---|---|
| confirmed | 患者对结构化回读明确确认（"记录为：呕吐1次，对吗？"→"对"），或来自结构化打卡控件 | 进入趋势计算与规则引擎 |
| explicit | 抽取值可在原文中直接定位（数值/枚举词匹配），且Schema校验通过，且双次抽取一致 | 进入趋势计算与规则引擎；Summary中标注"患者自报" |
| inferred | 值需推断（"吃得很少"→食欲下降），或双次抽取不一致但可合并 | 触发一次追问以升级为confirmed；未确认则不入趋势，仅入Timeline并标注 |
| uncertain | Schema校验失败 / 双次抽取冲突 / 上下文不足 | 不入趋势；转护士确认；疑似急症时按更高安全等级处理 |

判定信号全部为确定性检查：

1. Schema / 单位 / 取值范围校验；
2. 自洽校验：同一消息做两次独立抽取（不同采样温度或双模型）并比对；
3. 显式性检查：抽取值能否在患者原文中定位；
4. 患者确认回读。

### 6.3 数值分数的保留用途

Agent 输出 Schema 中的 `confidence` 数值字段保留，但重新定义为**启发式排序分**：仅用于队列排序和研发观测，不得作为任何治理阈值。治理阈值一律绑定 `confidence_tier`。

### 6.4 校准路径

试点期每周抽样人工复核（如 50 条），统计各 tier 的实际正确率，据此调整判定条件；复核结果写入审计日志。

**联动修改**：
- `03_data_model_fhir.md` §6.2：Observation 增加字段 `confidence_tier`，enum: `confirmed / explicit / inferred / uncertain`。
- `05_ai_agents.md` §9：Agent 输出 Schema 增加同名字段。

---

## 六、06 安全与治理：增补 §5.4 急症识别分层策略【新增】

**位置**：`06_safety_and_governance.md` §5 Emergency Workflow 内新增小节。原文只有"急症关键词"，对口语化中文的召回问题没有交代——而漏报一次急症是本产品最坏的失败模式。

急症识别以**召回率优先**，接受一定误报由护士队列消化。分三层：

1. **结构化直报层**：患者端每日打卡固定包含红旗症状快捷项（胸痛、呼吸困难、意识异常、大量出血等），一次点选即触发，不依赖任何NLP。
2. **词典规则层**：急症关键词词典包含口语变体（"胸口闷得慌""上不来气""眼前发黑""心口疼"），并配套否定与既往描述的排除规则（"没有胸痛""上个月胸痛过"）。词典由临床顾问审批、版本化管理。
3. **LLM分类层**：仅对前两层未命中的自由文本做分类。分类器输出"不确定"时**一律按急症处理**（不确定即升级原则）。

**漏报处置**：任何被证实的漏报作为安全事件复盘，样本回灌词典与分类测试集，48 小时内产出复盘记录。

---

## 七、06 安全与治理：新增 §13 合规与监管界定（中国）【新增】

**位置**：`06_safety_and_governance.md` 末尾新增。原 §11"法务和合规边界"只管对外话术，本节补齐实质合规立场。

### 13.1 伦理审查

任何涉及真实患者数据的试点（含只读集成），启动前须取得试点医院**伦理委员会批件**。知情同意书须说明：AI 处理方式、数据用途、退出机制。

### 13.2 个人信息保护（PIPL / 数据安全法）

- 健康数据属**敏感个人信息**：随访授权采用**单独同意**，记录同意版本与时间。数据模型联动：Patient 的 `consent_status` 旁增加 `consent_version` 字段。
- **数据不出院**：试点与部署版默认私有化部署，模型调用在院内或经审批的专有云内完成；任何外部 API 调用必须脱敏并留痕。
- **最小必要**：采集项以 Pathway 配置为准，不做开放式采集。
- 展示与日志脱敏（已有）。补充：患者查询、更正、删除权利的响应流程；与病历保存法规冲突时以法规为准，并向患者说明。

### 13.3 医疗器械属性界定

依据 NMPA《人工智能医用软件产品分类界定指导原则》（2021），本产品从设计上规避器械属性的关键论点：

- 不输出诊断结论、治疗方案、个体化用药建议（红线体系已有）。
- 风险分级 L0-L4 定位为**工作流优先级排序与转人工提示**，不构成临床判断；最终判断由医护做出并留痕。
- 急症提示为就医指引类固定话术，不含病因判断。

**行动项**：试点前完成属地药监部门的**分类界定咨询**并取得书面意见；若被界定为器械，则收缩相应功能（例如关闭风险分级对患者侧的一切展示）或评估注册路径。

**答辩口径**："我们按非器械边界设计，并把正式界定咨询作为试点的前置条件。"——这比"我们肯定不是器械"可信得多。

### 13.4 责任框架（与法务确认清单）

- 系统定位为"辅助信息工具"；患者端常驻免责声明与急救指引（已有）。
- 高风险 Alert 关闭强制记录责任人与理由（已有），作为责任界定依据。
- 待法务确认：漏报情形下的责任划分、与医院合同中的责任条款、保险安排。

---

## 八、新增文件 `docs/12_evaluation.md` 质量评测方案【新增】

以下为新文件全文。目的：把"AI好不好用"从主观判断变成可回归的数字，正面回应技术评委必问的"准确率多少"。

```markdown
# 12. 质量评测方案

抽取质量、Summary忠实度、急症识别与回归机制

## 1. 目的

验收标准（PRD §11）目前均为流程性验收。本方案补充质量验收：
抽取准确率、Summary忠实度、急症召回率，均可量化、可回归。

## 2. 抽取质量评测

### 2.1 测试集

3 条 Pathway 各 50 条合成中文患者消息，共 150 条，覆盖：

- 口语与错别字
- 一句多信息："有点恶心，早上就喝了点粥，没吐"
- 否定表达："没有吐，但一直恶心"
- 时间指代："昨晚"
- 模糊量："吃得很少"

由团队构造、临床顾问校订。每条标注期望的 Observation 集合
（code / value / unit / confidence_tier）。

### 2.2 指标

- 字段级 Precision / Recall / F1
- 数值与单位归一准确率
- 否定处理准确率

### 2.3 门槛

- 总体 F1 ≥ 0.85 方可进入真实患者试点
- 单类 F1 < 0.70 的 Observation 强制改走"追问确认"或结构化控件

## 3. Summary 忠实度评测

### 3.1 硬约束（自动化，进CI）

无证据 bullet 率 = 0。任何 bullet 缺少 evidence_refs 由
Safety Agent 阻断。

### 3.2 人工抽检（每版本 ≥ 20 份）

- 忠实度：bullet 与证据一致率 ≥ 98%
- 覆盖率：对照标注的"必须出现事件"清单，遗漏率 < 5%
- 幻觉率目标为 0，发现即安全事件

### 3.3 工具

LLM-as-judge 仅作初筛，金标准为人工复核。

## 4. 急症识别评测

### 4.1 测试集（100 条）

直接表达 30 / 口语变体 30 / 否定与既往干扰 30 / 边界模糊 10。

### 4.2 指标

- 阳性集召回率 ≥ 0.95（第一优先）
- 误报率以护士队列可承受为限，试点期实测标定
- "不确定 → 升级"路径覆盖率 100%

### 4.3 规则引擎

规则引擎为确定性逻辑，不进此评测；改用单元测试全覆盖：
给定 Observation 序列，断言触发 / 不触发结果。

## 5. 回归机制

Prompt 版本、模型版本、急症词典版本任一变更，必须全量重跑
第 2-4 节评测，结果写入审计。未达门槛不得发布。

## 6. 答辩使用

答辩前至少完成一轮真实评测，将 F1、召回率等实数填入 PPT。
"抽取 F1 0.87"远强于"我们做了安全设计"。
```

---

## 九、09 答辩材料：问答增补【新增】

**位置**：`09_pitch_materials.md` §9 安全问答之后追加。以下答案可直接照读。

**Q：医生凭什么会打开你们的 Summary？**
我们不赌医生行为改变。三条路径把打开成本压到接近零：HIS 工作站叫号联动弹出、复诊前夜一页版推送、试点期直接纸质打印夹入病历。同时第一阶段可量化的价值先放在护士侧——随访工时和SLA——医生侧用打开率和评分数据说话。

**Q：你们的置信度是模型自己报的吗？**
不是。模型自报分数不可校准，我们不用它做治理。治理使用四档 confidence_tier，由确定性信号判定：Schema校验、双次抽取一致性、原文可定位、患者确认回读。低档数据不进趋势计算，要么追问确认，要么转人工。

**Q：你们算医疗器械吗？**
我们按非器械边界设计：不诊断、不给治疗和用药建议；风险分级只做工作流排序和转人工提示，最终判断由医护做出并留痕。同时我们把属地药监的分类界定咨询作为试点前置条件，取得书面意见后才接触真实患者。

**Q：抽取准确率多少？急症会不会漏？**
（答辩前填入实测数字）我们有 150 条中文口语测试集，字段级 F1 为 __；急症识别是三层设计、召回优先：结构化红旗直报、口语变体词典、LLM分类兜底，100 条急症测试集召回率 __，且分类器"不确定"一律按急症升级。规则引擎部分是确定性逻辑，单元测试全覆盖。

**Q：为什么要多 Agent，一个大模型加工具不行吗？**
Demo 层面行，医院治理层面不行。Agent 对我们是权限、审计和降级的边界单元：随访 Agent 永远碰不到 Alert 关闭权限，Summary Agent 永远发不出患者消息。底座模型可以共享，边界不能共享。

---

## 十、10 Roadmap：两周执行计划调整【新增】

**位置**：`10_roadmap.md` §9

**Week 1 追加**：
- 构造抽取评测集（150 条）并完成首轮评测
- 构造急症识别测试集（100 条）

**Week 2 追加**：
- 合规一页纸（伦理 / PIPL / 器械界定的统一口径）
- 国内竞品对比页（核实后填入）
- 新增答辩问答演练（本修订包第九部分）
- 将实测 F1 / 召回率填入答辩 PPT

---

## 附：本次未改动但建议后续处理

1. **模型选型未交代**：私有化部署下用哪个可本地化的模型、抽取质量与成本的权衡，试点方案里迟早要回答。建议在评测集跑通后用同一集合对 2-3 个候选模型出对比数字。
2. **Observation 编码**：目前用内部编码（`symptom.nausea.severity`）。MVP 够用，但试点前应给出向 LOINC / SNOMED CT 或院内字典映射的预留方案，写一句进 03 数据模型的设计原则即可。
3. **付费方论证**：商业模式部分对"公立医院为什么买单"仍偏薄。可补充与互联网医院随访收费、公立医院绩效考核中随访相关指标的挂钩论证——需要查证最新政策后再写，不建议凭印象填。
