"""M6 placeholder. No Bitable integration is claimed in the local demo."""


class BitableStore:
    def __init__(self, *args, **kwargs):
        raise RuntimeError("BitableStore 尚未联调；请继续使用 SQLiteStore")

