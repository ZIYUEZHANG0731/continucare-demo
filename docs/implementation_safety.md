# 本地 Demo 安全边界

- 数据：只 Seed `P-DEMO-001 / 陈女士（合成）`，运行数据库被 `.gitignore` 排除。
- 用途：把患者原文转成可追溯 Observation 和工作流优先级；不是诊断系统。
- 风险：最终 L0/L2/L4 由确定性纯函数生成，Mock 抽取不直接决定治理动作。
- 急症：固定红旗表达触发固定急救提示，不输出疾病判断。
- 摘要：每条内容必须有 `evidence_refs`；推断内容只能进入“医生待确认”。
- 外部系统：飞书/Aily 默认为关闭；所有通知均明确标注 Mock 和未联调。
- EMR：医生审阅不会默认写入 EMR。

